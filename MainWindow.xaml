<Window x:Class="NanCrypt.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NanCrypt" Height="450" Width="800"
        Background="#121212" Foreground="#00FF41"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#00FF41"/>
            <Setter Property="BorderBrush" Value="#00FF41"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00FF41"/>
                                <Setter Property="Foreground" Value="#121212"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
        <Style TargetType="TextBlock">
             <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#00FF41"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                         <Border Background="{TemplateBinding Background}" BorderBrush="Transparent" Padding="5">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsChecked" Value="True">
                                 <Setter Property="FontWeight" Value="Bold"/>
                                 <Setter Property="Foreground" Value="#FFFFFF"/>
                             </Trigger>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter Property="Foreground" Value="#AAFFAA"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="NANCRYPT // SYSTEM SECURITY" FontSize="24" FontWeight="Bold" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect Color="#00FF41" BlurRadius="10" ShadowDepth="0"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- Tabs / Mode Selection -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <RadioButton x:Name="RadioEncrypt" Content="[ ENCRYPT ]" GroupName="Mode" IsChecked="True" 
                         FontSize="16" Margin="0,0,10,0" Checked="Mode_Checked"/>
            <RadioButton x:Name="RadioDecrypt" Content="[ DECRYPT ]" GroupName="Mode" 
                         FontSize="16" Margin="0,0,10,0" Checked="Mode_Checked"/>
            <RadioButton x:Name="RadioScan" Content="[ VIRUS SCAN ]" GroupName="Mode" 
                         Foreground="#FF0000" FontSize="16" Checked="Mode_Checked"/>
        </StackPanel>

        <!-- Main Content Area -->
        <Border Grid.Row="2" BorderBrush="#333333" BorderThickness="1" Background="#1E1E1E" CornerRadius="4">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="TARGET FILE:" Foreground="#888888" Margin="0,0,0,5"/>
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                     <CheckBox x:Name="ChkBatchMode" Content="BATCH MODE (PROCESS FOLDER)" Foreground="#00FF41" Checked="ChkBatchMode_CheckedChanged" Unchecked="ChkBatchMode_CheckedChanged" Margin="0,0,0,10"/>
                     <CheckBox x:Name="ChkArchive" Content="COMPRESS FOLDER TO SINGLE FILE (ZIP)" Foreground="#00FF41" Visibility="Collapsed" Checked="ChkArchive_CheckedChanged" Unchecked="ChkArchive_CheckedChanged" Margin="0,0,0,10"/>
                     
                     <Grid x:Name="PanelFileTypes" Visibility="Collapsed" Margin="0,0,0,10">
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="*"/>
                         </Grid.ColumnDefinitions>
                         <TextBlock Text="FILTER:" Foreground="#888888" VerticalAlignment="Center" Margin="0,0,10,0"/>
                         <ComboBox x:Name="CmbFilePattern" Grid.Column="1" SelectionChanged="CmbFilePattern_SelectionChanged" Height="25">
                             <ComboBoxItem Content="All Files (*.*)" Tag="*.*" IsSelected="True"/>
                             <ComboBoxItem Content="Images (*.jpg, *.png, *.bmp)" Tag="*.jpg;*.jpeg;*.png;*.bmp"/>
                             <ComboBoxItem Content="Documents (*.txt, *.pdf, *.docx)" Tag="*.txt;*.pdf;*.docx;*.doc"/>
                             <ComboBoxItem Content="Executables (*.exe, *.dll)" Tag="*.exe;*.dll"/>
                             <ComboBoxItem Content="Custom..." Tag="Custom"/>
                         </ComboBox>
                     </Grid>
                     <TextBox x:Name="TxtCustomPattern" Visibility="Collapsed" Margin="0,0,0,10" ToolTip="Enter extensions separated by semicolon (e.g. *.cpp;*.h)"/>

                     <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtFilePath" IsReadOnly="True" Text="Select target..."/>
                        <Button Content="..." Width="40" Margin="10,0,0,0" Click="BtnBrowse_Click" Grid.Column="1"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="PanelPassword" Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="ENCRYPTION KEY:" Foreground="#888888" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtPassword"/>
                </StackPanel>

                <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
                     <Button x:Name="BtnAction" Content="EXECUTE PROTOCOL" FontSize="18" Height="50" Click="BtnAction_Click"/>
                     <TextBlock x:Name="LblStatus" Text="READY." Margin="0,10,0,0" HorizontalAlignment="Center" Foreground="#888888"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <TextBlock Grid.Row="4" Text="v1.0.0 // NANCRYPT CORE ONLINE" HorizontalAlignment="Right" Margin="0,10,0,0" FontSize="10" Foreground="#444444"/>
    </Grid>
</Window>
